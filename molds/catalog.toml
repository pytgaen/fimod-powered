[molds.dockerfile]
description = "Generate a production-ready Dockerfile from a JSON/YAML descriptor"
path = "dockerfile/dockerfile.py"
readme = "dockerfile/README.md"
output_format = "txt"
hash = "511261be79db72929cc1619980ad68fa"
files = [
    "dockerfile/CONVERT_PROMPT.md",
    "dockerfile/templates/Dockerfile.j2",
]

[molds.download]
description = "Download a file from a URL (wget-like)"
path = "download/download.py"
readme = "download/README.md"
input_format = "http"
hash = "ec46ba23f79b8673e14d0ddf69b1af2a"

[molds.download.args]
out = "Output filename (defaults to the last path segment of the URL)"

[molds.gh_latest]
description = "Get the latest release tag or download URL from a GitHub repository"
path = "gh_latest/gh_latest.py"
readme = "gh_latest/README.md"
input_format = "http"
output_format = "txt"
options = ["no-follow"]
hash = "f93132dc5ee1e9f9dca83157b03281d4"

[molds.gh_latest.args]
asset = "Asset filename pattern - supports {tag} and {version} placeholders"
repo = "GitHub repository (owner/repo) - required for download URL"

[molds.html_report]
description = "Generate a standalone HTML report from JSON/CSV data"
path = "html_report/html_report.py"
readme = "html_report/README.md"
output_format = "txt"
hash = "fc4a6565b0042130c1c1c17f57ba0e6b"
files = ["html_report/templates/report.html.j2"]

[molds.html_report.args]
description = "Subtitle text below the title"
null-display = 'Display for null/missing values: "null"'
theme = 'Theme: "light" or "dark" (default: "light")'
timestamp = 'Generation timestamp shown in footer (e.g. "2024-01-15 10:30")'
title = 'Report title (default: "Report")'

[molds.poetry_migrate]
description = "Migrate a Poetry pyproject.toml to Poetry 2 or uv format"
path = "poetry_migrate/poetry_migrate.py"
readme = "poetry_migrate/README.md"
output_format = "toml"
hash = "50139a8dc2828fdc24945696f8e61352"
files = ["poetry_migrate/MIGRATE_FULL_PROMPT.md"]

[molds.poetry_migrate.args]
build = "Build backend for uv target: hatchling, setuptools, flit, pdm, uv (default: hatchling)"
index_strategy = "uv index-strategy: first-index (default), unsafe-best-match, unsafe-first-match"
target = "Migration target: poetry2 or uv (default: poetry2)"

[molds.skylos_to_gitlab]
description = "Convert Skylos dead code reports to GitLab Code Quality format"
path = "skylos_to_gitlab/skylos_to_gitlab.py"
readme = "skylos_to_gitlab/README.md"
output_format = "json"
hash = "be71d300ce2bc275d91e5ff833918f9d"

[molds.skylos_to_gitlab.args]
min-confidence = "Minimum confidence threshold (0-100). Items below this value are excluded."
