[tool.poetry]
name = "my_package"
version = "0.1.0"
description = "Editable git dep for uv"
authors = ["Dev <dev@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
my-lib = {git = "https://github.com/org/my-lib.git", branch = "main", develop = true}
