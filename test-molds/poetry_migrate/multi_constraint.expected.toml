[project]
name = "my_package"
version = "0.1.0"
description = "Multi-constraint deps"
dependencies = [
    "multi>=1.0.0,<2.0.0; python_version >= '3.8' and python_version < '3.11'",
    "multi>=2.0.0,<3.0.0; python_version >= '3.11'",
]
requires-python = ">=3.9,<4.0"

[[project.authors]]
name = "Dev"
email = "dev@example.com"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
