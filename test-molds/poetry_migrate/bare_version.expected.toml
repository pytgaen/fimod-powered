[project]
name = "my_package"
version = "1.0.0"
description = "Test bare version constraint"
dependencies = [
    "pinned-lib==1.0.0",
    "some-lib==2.28",
]
requires-python = ">=3.9.0,<4.0.0"

[[project.authors]]
name = "Dev"
email = "dev@example.com"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
