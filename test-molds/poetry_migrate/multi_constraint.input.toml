[tool.poetry]
name = "my_package"
version = "0.1.0"
description = "Multi-constraint deps"
authors = ["Dev <dev@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
multi = [
    {version = "^1.0", python = ">=3.8,<3.11"},
    {version = "^2.0", python = ">=3.11"},
]
