[tool.poetry]
name = "my_package"
version = "0.1.0"
description = "Per-dep markers"
authors = ["Dev <dev@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
backports-zoneinfo = {version = "^0.2", python = "<3.9"}
pywin32 = {version = "^306", platform = "win32"}
custom = {version = "^1.0", markers = "implementation_name == 'cpython'"}
combo = {version = "^2.0", python = ">=3.10", platform = "linux"}
