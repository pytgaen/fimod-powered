[project]
name = "my_package"
version = "0.1.0"
description = "URL dependency for uv"
dependencies = ["my-wheel"]
requires-python = ">=3.9,<4.0"

[[project.authors]]
name = "Dev"
email = "dev@example.com"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources.my-wheel]
url = "https://example.com/my_wheel-1.0-py3-none-any.whl"
